import { Hotel } from "lucide-react"

export const AccomodationIcon: React.FC= ({  }) => {
    return (
        <div>
            <Hotel className="w-4 text-[#7EC3F5]" strokeWidth={2} />
        </div>

    );
}