import { PlaneIcon } from "lucide-react";

export const FlightsIcon: React.FC= ({  }) => {
    return (
        <div>
             <PlaneIcon className="w-4 text-[#E38B4B] " />
        </div>

    );
}